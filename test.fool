let
    class A (a:bool, x:int){
        fun puppa:int()
            x;

        fun sub:int(b:bool)
            let
                var h:int = 10;
            in
                h;
    }

    var obj:A = new A(false, 5);
    var nullable:int = null;
in
    obj.puppa();